/**
 * Naming Conventions 1
 *
 * 1) 일반적으로 영어를 사용하며 문자와 숫자를 모두 사용할 수 있다.
 * 2) 특수기호는 언더바와 달러만 사용할 수 있다.
 * 3) 숫자로 시작할 수 없다.
 * 4) 키워드는 변수명으로 사용할 수 없다.
 */

let eric = "Eric";
var $ive = "아이브";
const _newJeans = "뉴진스";

console.log(eric);
console.log($ive);
console.log(_newJeans);

/**
 * Naming Conventions 2
 *
 * 1) camelCase - 대부분의 언어에서 사용 / 소문자로 시작, 띄어쓰기 후 첫 문자 대문자
 * 2) snake_case - 파이썬 등에서 사용 / 소문자만 사용, 띄어쓰기 대신에 언더바로 사용
 * 3) PascalCase - C#, 마이크로소프트 계열의 언어에서 사용 / 대문자로 시작, 띄어쓰기 후 첫 문자 대문자
 */
